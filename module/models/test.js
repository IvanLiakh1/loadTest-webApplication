class Test {
	constructor({ url, duration, concurrency, method, body, save, token }) {
		this.url = url;
		this.duration = duration;
		this.concurrency = concurrency;
		this.method = method;
		this.body = body;
		this.save = save;
		this.token = token;
	}
}
export default Test;
