class Test {
	constructor({ url, duration, concurrency, method, body, save }) {
		this.url = url;
		this.duration = duration;
		this.concurrency = concurrency;
		this.method = method;
		this.body = body;
		this.save = save;
	}
}
export default Test;
